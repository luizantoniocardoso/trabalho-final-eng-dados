# Arquivo de exemplo de variáveis de ambiente para o Airflow
# Copie este arquivo para .env e ajuste os valores conforme necessário

# Credenciais do Airflow UI
_AIRFLOW_WWW_USER_USERNAME=airflow
_AIRFLOW_WWW_USER_PASSWORD=airflow

# UID do Airflow (use 50000 ou execute: echo -e "AIRFLOW_UID=$(id -u)" no Linux/Mac)
AIRFLOW_UID=50000
